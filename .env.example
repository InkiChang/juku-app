# Development default. A phone must use the host LAN address, not 127.0.0.1.
VITE_API_BASE_URL=http://127.0.0.1:8998

# Optional production default. The in-app server setting can override this.
# VITE_PRODUCTION_API_BASE_URL=https://juku.example.com
